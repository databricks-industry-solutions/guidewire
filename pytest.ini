[pytest]
testpaths = testing/tests
pythonpath = .
filterwarnings =
    error
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    unit: Unit tests with mocks (fast)
    integration: Integration tests with real services (slower)
    slow: Slow tests that require full setup